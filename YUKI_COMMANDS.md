# YUKI 指令参考

## 触发 YUKI

| 方式 | 说明 |
|---|---|
| `@YUKI` | @ 机器人唤醒 |
| 引用回复 YUKI 的消息 | 回复 YUKI 的上一条消息 |
| `yuki <内容>` | 默认触发关键词（可在 `config.yaml` 修改） |
| 接续对话 | 上次互动后 180 秒内的消息自动视为续聊（可开关） |

---

## 续聊开关

| 指令 | 效果 |
|---|---|
| `开启续聊` / `开续聊` / `启用续聊` / `wake on` | 打开接续对话模式 |
| `关闭续聊` / `关续聊` / `禁用续聊` / `wake off` | 关闭接续对话模式 |

---

## 管理员审批（代码执行确认）

| 指令 | 效果 |
|---|---|
| `同意` / `批准` / `确认` / `approve` / `yes` / `y` | 批准当前代码执行请求 |
| `拒绝` / `驳回` / `取消` / `reject` / `deny` / `no` / `n` | 拒绝当前代码执行请求 |
| `允许本次任务` / `allow_task` / `批量` | 整个任务期间自动放行 |

可在指令后附加 8 位请求 ID（如 `同意 a1b2c3d4`）；引用审批卡片可免 ID。

---

## 管理员系统命令

| 指令 | 效果 |
|---|---|
| `/clear` | 清除当前会话（群/私聊）的上下文记忆 |
| `@prompt <内容>` / `/prompt <内容>` | 设置越狱指令（最高优先级，YUKI 不得拒绝） |
| `@prompt clear` / `/prompt clear` | 清除越狱指令 |
| `@reloadmd` / `/reloadmd` | 重新读取 YUKI_SPACE/YUKI.md（无需重启） |
| `@recall <自然语言>` | AI 理解意图并撤回消息 |
| `@ban <自然语言>` | AI 理解意图并禁言成员 |

---

## 其他

- 以 `/clear` 开头的消息会被静默忽略（不触发 YUKI）

---

## YUKI 可用工具（自然语言调用）

通过对话让 YUKI 使用以下能力：

| 功能 | 说明 |
|---|---|
| 文件操作 | 列出、读取、写入、删除工作空间内的文件/目录 |
| 代码执行 | 运行 Python / Shell 命令（需管理员确认） |
| 群管理 | 禁言、撤回消息、查群成员列表、改群名片等（不能踢人/退群） |
| 联网搜索 | Bing 搜索实时信息 |
| 发送消息 | 向群发送消息（可 @ 成员或 @ 全体） |
| 发送文件/图片 | 把工作空间里的文件/图片发送到群或私聊 |
| 用户档案 | 查看/记录群成员信息 |
| 接收附件 | 用户发的图片/文件自动保存到工作空间 `received/` 目录 |